import 'package:flutter/material.dart';

import '../constants/colors.dart';

ButtonStyle elevatedButtonTheme() {
  return ElevatedButton.styleFrom(
      foregroundColor: textElevatedColor, backgroundColor: tsecondaryColor);
}
